/*
 * TODO: Remove This File
 * this enum is only used by the collector service.
 * we can remove this by adding how the field is collected from the collector service or the collector configuration
 */

export enum FieldType {
    NORMAL,
    FILE,
    DYNAMIC
}